# CDK Core
aws-cdk-lib>=2.100.0
constructs>=10.0.0

# CDK Alpha modules for experimental features
# aws-cdk.aws-lambda-python-alpha>=2.100.0a0

# Python dependencies for Lambda functions
boto3>=1.35.0
botocore>=1.35.0
# Runtime (Lambda) dependencies — kept in sync with
# layers/valkey-glide-layer/requirements.txt and PINNED exactly so the deployed
# artifact is reproducible and auditable. (valkey-glide in particular reorganized
# its package at 2.1.0: glide.protobuf -> glide_shared.protobuf, which silently
# broke imports — so the version is fixed.) Bump deliberately and re-verify.
valkey-glide==2.4.1
aws-lambda-powertools[all]==3.0.0
pydantic==2.13.4
asyncio-throttle==1.0.2
python-dateutil==2.9.0.post0
nest-asyncio==1.6.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
# Bumped from 2.28.1: CVE-2023-32681 (Proxy-Authorization leak on cross-host
# redirect). Used by the testing/validation utilities, not the Lambda runtime path.
requests>=2.32.4