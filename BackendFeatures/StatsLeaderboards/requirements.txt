# CDK Core
aws-cdk-lib>=2.100.0
constructs>=10.0.0

# CDK Alpha modules for experimental features
# aws-cdk.aws-lambda-python-alpha>=2.100.0a0

# Python dependencies for Lambda functions
boto3>=1.35.0
botocore>=1.35.0
# Pinned: valkey-glide reorganized its package at 2.1.0
# (glide.protobuf -> glide_shared.protobuf); pin exactly to keep the layer
# reproducible and imports in sync. Bump deliberately and re-verify imports.
valkey-glide==2.4.1
aws-lambda-powertools[all]==3.0.0
pydantic>=2.5.0
asyncio-throttle>=1.0.2
python-dateutil>=2.8.2
nest-asyncio>=1.5.8

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
requests==2.28.1